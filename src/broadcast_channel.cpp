#include "broadcast_channel.h"

//Default constructor - initialize the port member
broadcast_channel::broadcast_channel(int port) : port(port) { }

//Default destructor - nothing to do
broadcast_channel::~broadcast_channel(void){ }

